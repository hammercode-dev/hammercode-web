export default async function UserLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* <h1>Temporary Layout For User</h1> */}
      <div>{children}</div>
    </div>
  );
}
